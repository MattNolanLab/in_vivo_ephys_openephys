#%%
import numpy as np
import matplotlib.pylab as plt

#%%
compareRange = 20000
s = np.zeros((2000))
for lag in range(2000):
    vRisig = 
    s[lag]=np.sum(vsync[lag:lag+compareRange] & esync[:compareRange])

#%%
plt.plot(s)

#%%
